# Rig part sources

Put the approved transparent SVG or PNG body-part sources here. Filenames must
match `metadata/pet_manifest.json`. This template contains no placeholder body
parts: a production pack is valid only after every required source is present.
